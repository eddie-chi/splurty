<!DOCTYPE html>
<html>
<head>
  <title>Splurty | Random Quote Generator</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
  <div class="col-xs-3">
    <%= link_to root_path, class: 'logo' do %>
   <div class="circle">
      <br />
      Laugh
      <br />
      Out
      <br />
      Loud
    </div>
    <% end %>
  </div>
  <div class="col-sm-4 hidden-xs slogan"> <!-- Slogan -->
    Splurting out unsolicited advice about funny jokes since 2016
  </div>
  <div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links">
    <!-- Button trigger modal -->
    <%= link_to 'About', about_path %>
    &nbsp;&nbsp;
    <%= link_to 'Contribute', '#', data: {toggle: 'modal', target: '#myModal'} %>
  </div>
</header>

<!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Add your own quote</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
        <div class="modal-body">
            <%= f.input :saying, label: "What is the saying you want to add?", placeholder: "Blind is he who only sees one side" %>
            <br />
            <%= f.input :author, label: "Who is the author of the quote?", placeholder: "Jeff Stein" %>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Create', class: 'btn btn-danger' %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
<!-- End Modal -->
<hr />
<br class="clear" />
<div class="container">
  <% if flash[:error].present? %>
    <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
      <%= flash[:error].html_safe %>
    </div>
    <% end %>
  <%= yield %>
</div>
<div class="footer"> <!-- Footer -->
  <div class="container">
    <div class="col-xs-6">
      <%= link_to "This isn't enough, I need more", root_path, class: 'next-quote' %>
     </div>
     <div class="col-xs-6 footer-blurb">
       <small>Proudly Made by Eddie K.</small>
       <br />
       <small>Want to build your own Splurty Web App?</small>
       <smalll>
        Learn how to code <a href="http://thefirehoseproject.com">here</a>
      </small>
     </div>
  </div>
</div>
</body>
</html>
